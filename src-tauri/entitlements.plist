<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Hardened Runtime Entitlements for macOS Notarization -->
	
	<!-- Allow JIT compilation (needed for JavaScript engines) -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	
	<!-- Allow unsigned executable memory (needed for some frameworks) -->
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	
	<!-- Disable library validation (allows loading of non-Apple signed libraries) -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	
	<!-- Network entitlements -->
	<key>com.apple.security.network.client</key>
	<true/>
	<key>com.apple.security.network.server</key>
	<true/>
	
	<!-- File access entitlements -->
	<key>com.apple.security.files.user-selected.read-write</key>
	<true/>
	
	<!-- Microphone access (if needed for future features) -->
	<!-- Uncomment if you need microphone access -->
	<!-- <key>com.apple.security.device.audio-input</key> -->
	<!-- <true/> -->
	
	<!-- Camera access (if needed for future features) -->
	<!-- Uncomment if you need camera access -->
	<!-- <key>com.apple.security.device.camera</key> -->
	<!-- <true/> -->
</dict>
</plist>
