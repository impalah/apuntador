<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Enable edge-to-edge mode for Android 35+ -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Support for immersive mode -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">false</item>
        <!-- Enable edge-to-edge mode for Android 35+ -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!--
          Android 16 (API 36) ignores windowOptOutEdgeToEdgeEnforcement above, so the
          system status/nav bars become transparent regardless. Without an explicit dark
          windowBackground, the DayNight parent theme's light-mode default (white/light
          gray) shows through behind them instead of the app's dark UI.
        -->
        <item name="android:windowBackground">#1E1E1E</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Enable edge-to-edge mode for Android 35+ -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>